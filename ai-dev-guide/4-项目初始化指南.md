# 项目初始化指南

## 🎯 阶段目标

学习如何初始化一个完整的 Web 项目，包括项目结构搭建、依赖安装和基础配置。

## 🔍 什么是项目初始化？

项目初始化是指：
- 创建项目的基本目录结构
- 配置项目的基础设置
- 安装必要的依赖包
- 建立项目的初始文件

## 📋 项目初始化步骤

### 步骤1：选择项目类型

**向 AI 提问示例：**
- "如何初始化一个 Next.js 项目？"
- "如何创建一个 React + TypeScript 项目？"
- "如何初始化一个 Vue 3 项目？"

### 步骤2：创建项目目录

**向 AI 提问示例：**
- "Next.js 项目的目录结构应该如何组织？"
- "React 项目的文件结构最佳实践是什么？"
- "Vue 项目的标准目录结构是什么？"

### 步骤3：配置项目设置

**向 AI 提问示例：**
- "如何配置 TypeScript 项目？"
- "如何设置 Tailwind CSS 配置文件？"
- "如何配置 ESLint 和 Prettier？"

### 步骤4：安装依赖

**向 AI 提问示例：**
- "Next.js 项目需要安装哪些依赖？"
- "如何安装和配置 React Router？"
- "如何添加状态管理库？"

## 🚀 具体项目初始化指南

### Next.js 项目初始化

**向 AI 提问示例：**
- "如何使用 create-next-app 创建包含 TypeScript 和 Tailwind CSS 的项目？"
- "Next.js 14 的 App Router 如何使用？"
- "如何配置 Next.js 的环境变量？"

**初始化步骤：**

1. **创建项目**：
   ```bash
   # 交互式创建
   npx create-next-app@latest
   
   # 非交互式创建（推荐）
   npx create-next-app@latest my-app --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
   ```

2. **项目结构**：
   ```
   my-app/
   ├── app/             # App Router 目录
   │   ├── page.tsx     # 首页
   │   ├── layout.tsx   # 布局
   │   └── globals.css  # 全局样式
   ├── components/      # 组件
   ├── lib/             # 工具函数
   ├── public/          # 静态资源
   ├── src/             # 源代码目录
   ├── .eslintrc.json   # ESLint 配置
   ├── .gitignore       # Git 忽略文件
   ├── next.config.js   # Next.js 配置
   ├── package.json     # 项目配置
   ├── postcss.config.js # PostCSS 配置
   ├── tailwind.config.ts # Tailwind 配置
   └── tsconfig.json    # TypeScript 配置
   ```

3. **安装额外依赖**：
   ```bash
   # 安装 UI 组件库
   npm install @radix-ui/react-icons
   
   # 安装状态管理
   npm install zustand
   
   # 安装 HTTP 客户端
   npm install axios
   ```

### React + Vite 项目初始化

**向 AI 提问示例：**
- "如何使用 Vite 创建 React + TypeScript 项目？"
- "Vite 项目的目录结构最佳实践是什么？"
- "如何配置 Vite 的别名？"

**初始化步骤：**

1. **创建项目**：
   ```bash
   npm create vite@latest my-app -- --template react-ts
   ```

2. **项目结构**：
   ```
   my-app/
   ├── public/          # 静态资源
   ├── src/             # 源代码
   │   ├── components/  # 组件
   │   ├── hooks/       # 自定义 Hooks
   │   ├── utils/       # 工具函数
   │   ├── App.tsx      # 主应用
   │   ├── main.tsx     # 入口文件
   │   └── index.css    # 全局样式
   ├── .eslintrc.cjs    # ESLint 配置
   ├── .gitignore       # Git 忽略文件
   ├── index.html       # HTML 模板
   ├── package.json     # 项目配置
   ├── tsconfig.json    # TypeScript 配置
   ├── tsconfig.node.json # Node.js TypeScript 配置
   └── vite.config.ts   # Vite 配置
   ```

3. **安装额外依赖**：
   ```bash
   # 安装 React Router
   npm install react-router-dom
   
   # 安装 Tailwind CSS
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

### Vue 3 项目初始化

**向 AI 提问示例：**
- "如何使用 Vite 创建 Vue 3 + TypeScript 项目？"
- "Vue 3 项目的目录结构最佳实践是什么？"
- "如何配置 Vue Router 和 Pinia？"

**初始化步骤：**

1. **创建项目**：
   ```bash
   npm create vite@latest my-app -- --template vue-ts
   ```

2. **项目结构**：
   ```
   my-app/
   ├── public/          # 静态资源
   ├── src/             # 源代码
   │   ├── assets/      # 资源文件
   │   ├── components/  # 组件
   │   ├── router/      # 路由
   │   ├── stores/      # 状态管理
   │   ├── views/       # 页面
   │   ├── App.vue      # 主应用
   │   └── main.ts      # 入口文件
   ├── .eslintrc.cjs    # ESLint 配置
   ├── .gitignore       # Git 忽略文件
   ├── index.html       # HTML 模板
   ├── package.json     # 项目配置
   ├── tsconfig.json    # TypeScript 配置
   ├── tsconfig.node.json # Node.js TypeScript 配置
   └── vite.config.ts   # Vite 配置
   ```

3. **安装额外依赖**：
   ```bash
   # 安装 Vue Router
   npm install vue-router@4
   
   # 安装 Pinia（状态管理）
   npm install pinia
   
   # 安装 Tailwind CSS
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

## 🛠️ 基础配置指南

### TypeScript 配置

**向 AI 提问示例：**
- "如何配置 TypeScript 项目？"
- "tsconfig.json 的常见配置选项有哪些？"
- "如何设置 TypeScript 路径别名？"

**配置示例：**
```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### Tailwind CSS 配置

**向 AI 提问示例：**
- "如何配置 Tailwind CSS？"
- "tailwind.config.ts 的常见配置有哪些？"
- "如何添加自定义颜色和字体？"

**配置示例：**
```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: '#3b82f6',
        secondary: '#10b981',
        dark: '#1f2937',
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
    },
  },
  plugins: [],
}

export default config
```

### ESLint 和 Prettier 配置

**向 AI 提问示例：**
- "如何配置 ESLint 和 Prettier？"
- "ESLint 和 Prettier 如何协同工作？"
- "如何设置代码格式化规则？"

**配置示例：**

```json
// .eslintrc.json
{
  "extends": [
    "next/core-web-vitals",
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "plugin:prettier/recommended"
  ],
  "rules": {
    "@typescript-eslint/no-unused-vars": "error",
    "@typescript-eslint/no-explicit-any": "warn"
  }
}
```

```json
// .prettierrc
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2
}
```

## 📁 项目结构最佳实践

### 前端项目通用结构

```
project/
├── public/          # 静态资源
├── src/             # 源代码
│   ├── components/  # 可复用组件
│   ├── hooks/       # 自定义 Hooks
│   ├── utils/       # 工具函数
│   ├── types/       # TypeScript 类型定义
│   ├── assets/      # 图片、图标等
│   ├── styles/      # 全局样式
│   ├── pages/       # 页面（传统路由）
│   ├── app/         # 页面（App Router）
│   ├── router/      # 路由配置
│   └── stores/      # 状态管理
├── .eslintrc.json   # ESLint 配置
├── .prettierrc      # Prettier 配置
├── package.json     # 项目配置
└── tsconfig.json    # TypeScript 配置
```

### 文件命名规范

**向 AI 提问示例：**
- "前端项目的文件命名规范是什么？"
- "组件文件应该如何命名？"
- "TypeScript 类型文件的命名约定是什么？"

**命名建议：**
- **组件文件**：PascalCase（如 `Button.tsx`）
- **工具函数**：camelCase（如 `formatDate.ts`）
- **类型定义**：PascalCase + `.types.ts`（如 `User.types.ts`）
- **样式文件**：kebab-case（如 `global-styles.css`）

## 🎨 创建基础组件

### 示例：创建一个 Button 组件

**向 AI 提问示例：**
- "如何创建一个可复用的 Button 组件？"
- "如何为组件添加 TypeScript 类型？"
- "如何使用 Tailwind CSS 样式化组件？"

**组件示例：**

```tsx
// components/Button.tsx
import React from 'react'

interface ButtonProps {
  children: React.ReactNode
  variant?: 'primary' | 'secondary' | 'outline'
  size?: 'sm' | 'md' | 'lg'
  onClick?: () => void
  disabled?: boolean
  className?: string
}

export const Button: React.FC<ButtonProps> = ({
  children,
  variant = 'primary',
  size = 'md',
  onClick,
  disabled = false,
  className = '',
}) => {
  const variantClasses = {
    primary: 'bg-primary text-white hover:bg-primary/90',
    secondary: 'bg-secondary text-white hover:bg-secondary/90',
    outline: 'border border-gray-300 text-gray-700 hover:bg-gray-50',
  }

  const sizeClasses = {
    sm: 'px-3 py-1 text-sm',
    md: 'px-4 py-2 text-base',
    lg: 'px-6 py-3 text-lg',
  }

  return (
    <button
      onClick={onClick}
      disabled={disabled}
      className={`
        ${variantClasses[variant]}
        ${sizeClasses[size]}
        rounded-md font-medium transition-colors
        disabled:opacity-50 disabled:cursor-not-allowed
        ${className}
      `}
    >
      {children}
    </button>
  )
}
```

## 🚀 启动开发服务器

**向 AI 提问示例：**
- "如何启动 Next.js 开发服务器？"
- "如何运行 React 开发服务器？"
- "如何启动 Vue 开发服务器？"

**启动命令：**

```bash
# Next.js 项目
npm run dev

# React + Vite 项目
npm run dev

# Vue 3 项目
npm run dev
```

## 🎓 练习任务

### 任务1：初始化 Next.js 项目

**向 AI 提问：**
"请帮我初始化一个 Next.js 14 项目，包含以下配置：
1. TypeScript 支持
2. Tailwind CSS
3. ESLint 和 Prettier
4. App Router
5. 路径别名 @/*

请提供详细的步骤和命令。"

### 任务2：创建项目基础结构

**向 AI 提问：**
"我已经初始化了一个 React + TypeScript 项目，现在需要创建合理的项目结构。请推荐一个完整的目录结构，并说明每个目录的用途。"

## 📚 参考资源

- [Next.js 官方文档](https://nextjs.org/docs)
- [React 官方文档](https://react.dev/learn)
- [Vue 3 官方文档](https://vuejs.org/guide/introduction.html)
- [Tailwind CSS 官方文档](https://tailwindcss.com/docs)

---

**下一步：** [代码编写指南](5-代码编写指南.md)